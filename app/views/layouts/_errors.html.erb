<% if object.errors.any? %>
	<div id="error_explanation">
    <div class="alert alert-danger">
    	<%= pluralize(object.errors.count,"error") %> prohibited this obj from being saved:
    </div>
    <ul>
    	<% object.errors.full_messages.each do |obj| %>
    		<li><%= obj %></li>
    	<% end %>
    </ul>
	</div>
<% end %>