<h1>New book</h1>
<%= form_for(book) do |f| %>
	
	<%= f.label :title %></br>
	<%= f.text_field :title, autofocus: true %></br>

	<%= f.label :opinion %></br>
	<%= f.text_area :body, autofucus: true %>

	<%= f.submit %>
<% end %>