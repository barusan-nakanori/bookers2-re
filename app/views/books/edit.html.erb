<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>

	<%= f.label :title %></br>
	<%= f.text_field :title %></br>

	<%= f.label :opinion %></br>
	<%= f.text_area :body %></br>

	<%= f.submit %>

<% end%>
<%= link_to 'Show',book_path(@book) %> | <%= link_to 'Back',books_path %>